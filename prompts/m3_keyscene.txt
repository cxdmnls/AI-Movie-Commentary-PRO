任务：生成或修复 movie_info.json（mode={mode}）。

必须遵守以下规则：
1) 必须输出严格 JSON，不得输出任何解释文字。
2) key_scenes 场景数量必须在 {min_scene_count}-{max_scene_count} 之间，并尽量接近 {target_scene_count}。
3) scene_id 必须从 1 开始连续递增。
4) video_clip.start/end 必须为 "HH:MM:SS.m" 格式（如 "00:01:35.6"），精确到 0.1 秒。
5) 场景时间戳必须严格按时间顺序单调递增，后一个场景的 start 必须大于前一个场景的 end。
6) 场景划分规则：先将电影按时间顺序划分为 N 个连续不重叠的时间段（每个 scene 的 video_clip.start 必须等于上一个 scene 的 video_clip.end 或更晚），再为每个时间段匹配对应的 summary。video_clip.start 必须是该时间段内第一条字幕的实际时间，video_clip.end 必须是最后一条字幕的实际时间。
7) 禁止直接把字幕均分，必须按实际剧情话题转换点划分场景。
8) scene 的 summary 必须能体现该时间段内的核心剧情事件。
9) 【关键约束】sample_dialogue 必须严格从该 scene 的 video_clip 时间范围内的字幕中选取，每句台词的每个字都必须在这个时间范围内出现过。不允许选取范围外的台词！！！
10) phase 必须为以下之一：setup, inciting_incident, rising_action, midpoint, climax, resolution。
11) 【必须自检】输出前验证：每个 scene 的每句 sample_dialogue 都必须能在该 scene 的 video_clip 时间范围内的字幕文本中找到。如果找不到，必须替换为范围内能找到的台词。

字幕原文（SRT 解析结果，包含 start/end/text）：
{subtitles_json}

输出 JSON schema：
{output_schema_json}